<% if tags&.any? %>
  <div class="<%= css_class %> flex flex-wrap gap-1.5">
    <% tags.each do |tag| %>
      <span class="badge badge-sm badge-neutral"><%= tag.name %></span>
    <% end %>
  </div>
<% end %>

