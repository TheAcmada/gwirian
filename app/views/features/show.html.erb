<% content_for :title, "#{@feature.title} - #{@project.name}" %>
<div class="max-w-3xl mx-auto px-4 py-12">
  <%= render Features::HeaderComponent.new(feature: @feature, project: @project) %>
  <%= render partial: "scenarios/scenarios", locals: { feature: @feature, project: @project } %>
</div>
